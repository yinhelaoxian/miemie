image-cropper {
  width: 100%;
  height: 400px;
  background: transparent; /* 改为透明背景，避免黑色遮挡 */
  position: relative;
  z-index: 10; /* 确保裁剪控件在合理图层 */
}
button {
  margin: 20px;
  background: #07c160;
  color: #fff;
  z-index: 20; /* 确保按钮在裁剪控件之上 */
}