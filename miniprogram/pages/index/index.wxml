<!-- 拍照按钮 -->
<button bindtap="takePhoto">拍照录入错题</button>

<!-- 剪裁组件（假设为image-cropper） -->
<image-cropper
  wx:if="{{showCropper}}"
  src="{{cropperSrc}}"
  bind:confirm="onCropConfirm"
  bind:cancel="showCropper = false"
/>